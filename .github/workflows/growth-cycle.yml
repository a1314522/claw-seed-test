name: CLAW Growth Cycle
on:
  push:
    branches: [main]

permissions:
  contents: write
  issues: write

jobs:
  grow:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
    - uses: actions/checkout@v4
    - name: Test
      run: echo "Test passed"
