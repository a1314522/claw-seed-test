<UserControl x:Class="AssetManagementSystem.Desktop.Views.PurchaseEditPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:AssetManagementSystem.Desktop.ViewModels"
             Background="{DynamicResource BackgroundBrush}"
>
    <UserControl.DataContext>
        <vm:PurchaseListViewModel/>
    </UserControl.DataContext>
    <Grid Margin="24">
        <TextBlock Text="采购编辑页面 - 占位" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource ForegroundBrush}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
    </Grid>
</UserControl>