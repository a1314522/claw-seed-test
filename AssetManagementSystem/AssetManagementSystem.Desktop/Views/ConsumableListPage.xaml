<UserControl x:Class="AssetManagementSystem.Desktop.Views.ConsumableListPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:AssetManagementSystem.Desktop.ViewModels"
             Background="{DynamicResource BackgroundBrush}"
>
    <UserControl.DataContext>
        <vm:ConsumableListViewModel/>
    </UserControl.DataContext>
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="消耗品管理" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,16"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <Button Content="➕ 新增消耗品" Command="{Binding AddConsumableCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,8,0"/>
            <Button Content="📤 领用记录" Command="{Binding ViewUsageCommand}" Style="{StaticResource SecondaryButtonStyle}"/>
        </StackPanel>
        <Border Grid.Row="2" Style="{StaticResource CardStyle}">
            <DataGrid ItemsSource="{Binding Consumables}" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" BorderThickness="0" Background="Transparent">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="名称" Binding="{Binding Name}" Width="180"/>
                    <DataGridTextColumn Header="规格" Binding="{Binding Specification}" Width="120"/>
                    <DataGridTextColumn Header="当前库存" Binding="{Binding StockQuantity}" Width="100"/>
                    <DataGridTextColumn Header="单位" Binding="{Binding Unit}" Width="80"/>
                    <DataGridTextColumn Header="安全库存" Binding="{Binding MinStock}" Width="100"/>
                    <DataGridTextColumn Header="存放位置" Binding="{Binding StorageLocation}" Width="120"/>
                    <DataGridTemplateColumn Header="操作" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="✏️" Style="{StaticResource IconButtonStyle}" Command="{Binding DataContext.EditConsumableCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"/>
                                    <Button Content="➖" Style="{StaticResource IconButtonStyle}" Margin="4,0,0,0" Command="{Binding DataContext.IssueCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>